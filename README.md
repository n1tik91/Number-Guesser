# Number-Guesser in GO

## Random Number Generation: 

### Uses (math/rand) with a seed based on the current time to ensure randomness.
## User Input: 
### Reads guesses from the user via the (fmt.Scan) function.
